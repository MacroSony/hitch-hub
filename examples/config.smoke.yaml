data_dir: .remote-agent-hub-smoke
default_cwd: ..
agent_turn_timeout_ms: 5000

users:
  smoke:
    telegram_ids: []
    allowed_roots:
      - ..

channels:
  fake:
    enabled: true
  telegram:
    enabled: false
    bot_token_env: TELEGRAM_BOT_TOKEN
    allowed_chat_ids: []

agents:
  pi:
    command: pi
    default_args:
      - --mode
      - rpc
    default_policy: ask
